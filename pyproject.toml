[project]
name = "iptv-ultimate"
version = "0.1.0"
description = "Legal-by-default IPTV playlist builder, validator, deduper, and static player."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
iptv-ultimate = "iptv_ultimate.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
