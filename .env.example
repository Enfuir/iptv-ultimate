# IPTV Ultimate local notes

- Add legal M3U/M3U8 source URLs to `data/sources.local.json`.
- Keep paid, stolen, or unauthorized streams out of this repo.
- Use `python -m iptv_ultimate.cli build --validate --working-only` before publishing generated playlists.
